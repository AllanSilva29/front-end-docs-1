---
title: 'Design Responsivo e Media Queries'
description: 'Aprenda os princípios do Design Responsivo e como usar Media Queries para criar layouts que se adaptam a diferentes tamanhos de tela.'
---

import { Aside, Card, CardGrid } from '@astrojs/starlight/components';

Hoje, os usuários acessam a web a partir de uma vasta gama de dispositivos: celulares, tablets, laptops, desktops e até televisões. **Design Responsivo** é a prática de projetar e desenvolver uma interface que se adapta e oferece uma ótima experiência de usuário em qualquer tamanho de tela.

O objetivo não é criar um site diferente para cada dispositivo, mas sim um único site flexível que "responde" ao ambiente do usuário.

## Os Pilares do Design Responsivo

Três conceitos técnicos formam a base do design responsivo.

<CardGrid>
	<Card title="🌊 Layouts Fluidos">
		Em vez de usar larguras fixas em pixels (`px`), usamos unidades relativas como porcentagens (`%`), `vw` (largura da viewport) e a unidade `fr` no Grid. Isso permite que o layout se estique ou se comprima naturalmente com o tamanho da tela.
	</Card>
	<Card title="🖼️ Imagens Flexíveis">
		Imagens também precisam ser responsivas. Uma técnica simples é usar `max-width: 100%;` e `height: auto;`. Isso garante que a imagem nunca ultrapasse a largura de seu contêiner pai, encolhendo conforme necessário, mas sem distorcer.
	</Card>
    <Card title="📱 Media Queries">
		Esta é a ferramenta mais poderosa do CSS para responsividade. **Media Queries** permitem aplicar blocos de estilos CSS somente quando certas condições são atendidas, como a largura da tela atingir um determinado tamanho.
	</Card>
</CardGrid>

## A Meta Tag `viewport`

Antes de escrever qualquer Media Query, você precisa garantir que a seguinte meta tag esteja no `<head>` do seu HTML:

```html
<meta name="viewport" content="width=device-width, initial-scale=1.0">
```

Sem ela, os navegadores de dispositivos móveis tentarão renderizar a página como se estivessem em um desktop, resultando em uma página "diminuída" e ilegível. Esta tag diz ao navegador para ajustar a largura da página à do dispositivo e definir o nível de zoom inicial para 100%.

## Como as Media Queries Funcionam

A sintaxe de uma Media Query é simples. Você envolve um bloco de regras CSS dentro de um bloco `@media`.

```css
/* Estilos base (para telas pequenas) */
.container {
  width: 100%;
}

/* Quando a largura da tela for 768px OU MAIS,
   os estilos dentro deste bloco serão aplicados. */
@media (min-width: 768px) {
  .container {
    width: 750px;
    margin: 0 auto; /* Centraliza o contêiner */
  }
}
```
Os pontos onde o layout "quebra" e precisa de novos estilos são chamados de **breakpoints**.

### A Abordagem "Mobile First"

Uma das melhores práticas no design responsivo é o **"Mobile First"**. A ideia é:

1.  Escreva os estilos base do seu site pensando na experiência em uma tela pequena (mobile). Geralmente, isso significa layouts de coluna única, fontes maiores e botões fáceis de tocar.
2.  Use Media Queries com `min-width` para **adicionar** complexidade e reorganizar o layout à medida que a tela aumenta.

Essa abordagem força você a focar no conteúdo essencial primeiro e resulta em um CSS mais limpo e performático, já que os dispositivos móveis não precisam baixar e processar os estilos complexos destinados a desktops.

<Card title="Exemplo: Layout de Cards Responsivo">
```html
<div class="card-container">
  <div class="card">Card 1</div>
  <div class="card">Card 2</div>
  <div class="card">Card 3</div>
</div>
```
```css
/* Estilos Mobile First: por padrão, é uma coluna única */
.card-container {
  display: flex;
  flex-direction: column;
  gap: 1rem;
}

/* Breakpoint para tablets */
@media (min-width: 600px) {
  .card-container {
    flex-direction: row; /* Muda para layout de linha */
    flex-wrap: wrap;     /* Permite que os cards quebrem */
  }
  .card {
    flex-basis: 48%; /* Ocupa pouco menos da metade para caber o gap */
  }
}

/* Breakpoint para desktops */
@media (min-width: 1024px) {
  .card {
    flex-basis: 32%; /* Ocupa cerca de um terço */
  }
}
```
</Card>

Com a combinação de layouts fluidos (Flexbox/Grid), imagens flexíveis e o poder das Media Queries, você pode criar experiências de usuário robustas e agradáveis em todo o espectro de dispositivos.



